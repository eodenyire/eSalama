# eSalama Development Environment — Docker Compose variables
# Copy this file to .env and fill in the values before running `docker compose up`

# PostgreSQL
POSTGRES_USER=esalama
POSTGRES_PASSWORD=esalama_secret
POSTGRES_DB=esalama

# JWT secret key (change this in any non-local environment)
SECRET_KEY=change-me-in-production
